# UID/GID for prometheus and grafana
# After changing, run: sudo chown -R <PUID>:<PGID> prometheus/data/ grafana/data/
PUID=1000
PGID=1000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000

# Generate with: openssl rand -base64 32
GRAFANA_ADMIN_PASSWORD=changeme

