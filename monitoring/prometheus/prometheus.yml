---
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Add node exporters here once servers are set up
  # - job_name: "mailserver"
  #   static_configs:
  #     - targets: ["<mailserver-ip>:9100"]
